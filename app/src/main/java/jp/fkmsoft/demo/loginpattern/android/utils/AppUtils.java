package jp.fkmsoft.demo.loginpattern.android.utils;

/**
 * Utility class for App
 */
public class AppUtils {
    public static boolean isEmpty(String value) {
        return value == null || value.length() == 0;
    }
}
